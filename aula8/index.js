const nome = 'Reinaldo de Jesus';
const sobrenome = 'Santos';
const idade = 53;
const peso = 90;
const altura = 1.85;

let imc; //pesso / por (altura * altura)
let anoNascimento;

imc = peso / (altura * altura);
console.log(`${nome} ${sobrenome} tem ${idade} anos, pesa  ${peso} kg`);
 console.log(`tem ${altura} e seu IMC é de ${imc}`);



