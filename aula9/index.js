/**
 * Dados Primitivos
 * String, number, null, undefined, boolean, symbol
*/
const nome = 'REINALDO'; //string
const nome1 = "Reinaldo"; //string
const nome2 = `Reinaldo`; // String

const num1 = 10; //number
const num2 = 10.50 //number

let nomeAluno; //undefined -> não aponta pra local nunhim na memoria
const sobreNome = null; //Nulo -> não aponta para local nenhum da memoria

const aprovado = true; //Boolean -> true ou false (lógico)

console.log(typeof nome, nome);

